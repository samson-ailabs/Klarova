# Changelog

All notable changes to this project are documented here. The format follows
[Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and the project adheres to
[Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added
- Repository scaffold: `uv` package layout, pinned LangChain/LangGraph v1 dependencies,
  ruff + mypy (strict) + pytest config, Apache-2.0 license with SPDX headers, docs
  skeleton (`ARCHITECTURE.md`, `ROADMAP.md`, ADR index + template), and CI
  (ruff + mypy + pytest).
